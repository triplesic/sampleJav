# Language setting
language: java

# Version number
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
  - oraclejdk8

# Mongo binds to 127.0.0.1 by default
services:
  - mongodb
  
build:
  ci:
    - java -version
